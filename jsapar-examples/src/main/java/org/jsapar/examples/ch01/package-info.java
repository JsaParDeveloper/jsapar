/**
 * Chapter 01 contains the examples for setting up the library and the library tools. 
 * 
 * <i>SetupLibraryExample</i><br>
 * This is the basic example for setting up the library. It explains how to setup the library and what
 * kind of options the user has when configuring the library.<br>
 *  
 * <i>SetupParserExample</i><br>
 * This example shows how to setup a parser tool and use it to parse a simple delimited file into
 * a Document object.
 *
 * <i>SetupParserEnhancedExample</i><br>
 * This example shows how to setup a parser tool with enhanced possibilities and use it to parse a 
 * simple delimited file into a Document object.
 * 
 * <i>SetupComposerExample</i><br>
 * This example shows how to setup a composer tool and use it to compose a Document object into a 
 * simple delimited file.
 *  
 * <i>SetupConverterExample<i/><br>
 * This example shows how to setup a converter tool and use it to convert a simple delimited file 
 * into a simple fixed width file.
 * 
 * <i>SetupSplitterExample</i><br>
 * This example shows how to setup a splitter tool and use it to split a simple delimited file 
 * into a simple fixed width file.
 *  
 */
package org.jsapar.examples.ch01;